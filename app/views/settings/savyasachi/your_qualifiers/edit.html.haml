- content_for :page_title do
  = t('qualifiers.yours.update')

= simple_form_for @qualifier, url: settings_your_qualifier_path do |f|
  = render 'form', f: f

  .actions
    = f.button :button, t('generic.save_changes'), type: :submit

%hr/

= link_to t('qualifiers.back'), settings_your_qualifier_path(@qualifier), class: 'button button-alternative-2 webapp-btn'
